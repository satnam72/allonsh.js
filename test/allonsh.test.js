import { makeDraggable } from '../src/allonsh.js';

// basic test
describe('makeDraggable', () => {
  it('should be a function', () => {
    // Just checking that makeDraggable is defined and is a function
    expect(typeof makeDraggable).toBe('function');
  });
});
